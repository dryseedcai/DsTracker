apply plugin: 'groovy'

dependencies {
    //gradle sdk
    compile gradleApi()
    //groovy sdk
    compile localGroovy()

    compile 'com.android.tools.build:gradle:2.2.2'

    compile 'javassist:javassist:3.12.1.GA'
}

repositories {
    jcenter()
    mavenCentral()
}